# 환경 변수 (절대 커밋 금지)
.env
.env.local
.env*.local

# Firebase Admin SDK 서비스 계정 (절대 커밋 금지 — 키 노출 시 즉시 회전 필요)
# hospital 은 환경변수만 사용해서 파일 자체가 없지만, 예방 차원에서 패턴 등재
serviceAccount*.json
service-account*.json
*-firebase-adminsdk-*.json
google-credentials*.json

# Next.js 빌드 결과물
.next/
out/

# 의존성
node_modules/

# 디버그
npm-debug.log*

# 기타
.DS_Store
.claude/
context.md

# RPi 로컬 캐시 (syncEMR 환자 마스터)
.cache/

.vercel
.mcp.json
.mcp.local.json
