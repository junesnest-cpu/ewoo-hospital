# 2026-05-05 세션 정리 — Next.js / React 메이저 버전업 단계 A·B

**범위**: Stage 3 후속(별도 작업) 으로 잡혀있던 메이저 버전업의 첫 두 단계.
14 → 15 → 16, 18 → 19 의 단일 점프 금지 원칙에 따라 4 단계로 쪼갬.

---

## 사전 점검 결과 (전 환경 그린)

| # | 항목 | 결과 |
|---|---|---|
| 1 | 로컬 Node | v24 |
| 2 | Cloud Functions Node engines | "20" (`functions/package.json`) |
| 3 | Vercel 빌드 Node | 24.x (`mcp__vercel__get_project` 확인) |
| 4 | RPi (ewoo-sync) Node | v20.20.2 / npm 10.8.2 / Debian 13 trixie (ssh 직접 확인) |
| 5 | 주요 deps React 19 호환성 | 모두 호환. firebase 12 peer dep 없음, mssql/firebase-admin server-only 무영향, html2canvas 1.4.1 미관리(Stage C 에서 `html2canvas-pro` 로 swap 예정) |
| 6 | Pages Router strict mode 영향 | 7 페이지 audit — 6 Low, 1 Med (`ward-timeline.js` highlight timer). 모든 `onValue`/`setInterval` cleanup 패턴 정상 |
| 7 | 단계 전략 | 4 단계 분할 (A·B 오늘, C·D 후속) |

**핵심 발견**: Pages Router 전용이라 Next 15/16 의 굵직한 신기능(Cache Components, async cookies, Turbopack) 모두 App Router 한정 → 마이그레이션 코드 변경 표면적 매우 좁음. 실제로 단계 A·B 통틀어 **package.json 외 코드 변경 0 줄**.

---

## 단계 A — Next 14.2.35 → 15.5.15 LTS + firebase-admin 13.7→13.8

**브랜치/커밋**: `chore/next15-react18` → `0bf657a`

### 변경
- `next` 14.2.35 → 15.5.15 (LTS, 15.5 backport line)
- `firebase-admin` 13.7.0 → 13.8.0
- 코드 변경 0 줄

### 검증
- 로컬 `next build` — 18 페이지 정적/동적 모두 통과 (3.6s)
- 로컬 `next dev` smoke — `/`, `/ward-timeline`, `/treatment` 콘솔 에러 0
- Vercel preview — `dpl_3WENzxyBXoVtX8jwCLEa78XGLkH1` READY (35초)
- 번들 변화: shared First Load 171 → 172 kB (+1, framework patch 분)

### 사전 존재 경고 (회귀 아님)
- `pages/_app.js:241/254` next/head 에 stylesheet (Noto Sans KR) — Next 14 부터 동일. `_document.js` 이전은 별건 후속.

---

## 단계 B — React 18 → 19.2.5

**브랜치/커밋**: `chore/react19` → `18b25a9`

### 변경
- `react` ^18 → ^19 (실설치 19.2.5)
- `react-dom` ^18 → ^19 (실설치 19.2.5)
- 코드 변경 0 줄

### 검증
- 로컬 `next build` — 18 페이지 통과 (1.78s, 더 빨라짐)
- 로컬 SSR smoke (PowerShell `Invoke-WebRequest`) — `/`, `/treatment`, `/ward-timeline`, `/consultation`, `/daily-board`, `/monthly`, `/therapy` 모두 200 응답, 컴파일 경고 0
- Vercel preview — `dpl_AgzLWMEfKN6Ca5Yt8tgNqvUiKd2D` READY (35.7초)
- 번들 변화 (예상된 React 19 framework 증가): shared 172 → 186 kB (+14), `chunks/framework` 44.8 → 59.7 kB (+15) — 신규 hooks/Suspense 인프라

### Peer dep 호환
- `next@15.5.15` 의 react peer: `^18.2.0 || 19.0.0-rc... || ^19.0.0` — 19 명시 지원

### 직접 영향 없음 확인
- Pages Router 코드는 `findDOMNode`/string refs/legacy context/`propTypes`/`defaultProps` 미사용
- 모든 `useEffect` 의 Firebase RTDB `onValue` 와 `setInterval`/`setTimeout` 이 cleanup 함수 반환 — strict mode double-mount 회귀 위험 없음

---

## 단계 C — html2canvas → html2canvas-pro 2.0.2

**브랜치/커밋**: `chore/html2canvas-pro` → `d3292fc`

### 변경
- `html2canvas` ^1.4.1 (4년 미관리) 제거
- `html2canvas-pro` ^2.0.2 (활발 관리, modern CSS oklch/color() 호환) 추가
- `pages/daily-board.js:477` import 한 줄 변경

### 영향 범위
- **`pages/daily-board.js:477`** — "스크린샷 → 클립보드" 동적 import 1곳만
- 🖨 인쇄 기능들(`daily`, `daily-board`, `therapy`, `treatment`, `monthly`) 은 모두 `window.print()` 사용 — html2canvas 무관, 영향 없음

### 검증
- 로컬 `next build` 18 페이지 통과, daily-board 사이즈 8.6 kB 동일 (dynamic import 라 First Load JS 무영향)
- 로컬 SSR `/daily-board` 200, 컴파일 경고 0
- Vercel preview `dpl_2xcH2VQtt7acyE4sfUqjb5TDNWi5` READY
- 사용자 캡처 기능 검증 후 "문제없음" 확인

### 결정 근거 — 같은 슬롯 진행
A·B 두 메이저 직후라도 C 는 **인쇄/캡처 라이브러리 swap 으로 framework 레이어와 완전 독립**. 회귀 시 원인 분리 분명. D (Next 15→16) 는 같은 framework 레이어라 다음 슬롯으로 보존.

---

## 부수 작업 — HOTFIX docs

**커밋**: `8151a42` (`docs/HOTFIX.md` 5/5 모바일 vercel.app 간헐 403 관찰 기록)

upgrade 와 무관한 docs 변경이라 단계 A 머지 직후 main 에 별도 커밋. Vercel Deployment Protection 의심, 미수정 — 재발 시 custom domain 연결 또는 Protection 비활성화 옵션.

---

## 프로덕션 배포 결과

| 커밋 | 배포 ID | 상태 | 시점 |
|---|---|---|---|
| `0bf657a` (Next 15) | `dpl_ENwKBFNBVMhqmwfSYimEd2LMpdN3` | READY | 단계 A 머지 직후 |
| `8151a42` (HOTFIX docs) | `dpl_GYWcusFGJXG1Lz1zZZqY9zJrk39b` | READY | 같은 슬롯 |
| `18b25a9` (React 19) | `dpl_HCqmZmvMnzvqXWgGyk3awdamD4q2` | READY | 단계 B 머지 직후 |
| `991d644` (세션 docs) | `dpl_ENhxjcRbs8asQuao8YE8UT1xKNgr` | READY | 단계 C 진행 직전 |
| `d3292fc` (html2canvas-pro) | `dpl_EGDUvCpKUXhNLpVFjvVnPttYC7Cr` | READY | 단계 C 머지 직후 |

다섯 배포 모두 무사고. 사용자가 각 단계마다 PC + 모바일 검증 후 "문제없음" 확인.

**Dependabot 경고**: 7건 (2 high, 4 moderate, 1 low) → **2건 (1 moderate, 1 low)** — Next 15 + firebase-admin 13.8 효과로 critical/high 전부 해소.

---

## 운영 모니터링 체크리스트 (다음 슬롯 시작 전 24~48h)

- [ ] **React 19 strict mode 영향** — `ward-timeline.js` highlight 깜박임이 평소보다 지연·중복 깜박임 증상 보이는지 (BLINK_MS 타이머가 useEffect double-mount 로 중첩될 가능성 사전 audit 에서 Med)
- [ ] **인쇄 / 캡처 기능** — `treatment` 🖨, `daily-board` html2canvas 캡처 — React 19 환경에서도 정상 동작 (Stage C 에서 `html2canvas-pro` 로 swap 시 기준점이 됨)
- [ ] **Vercel runtime logs** — `[auth-enforce]` / `unauthorized` 급증 여부 (이전 모바일 첫 진입 401 burst 패턴 재발 모니터링)
- [ ] **RTDB 데이터 정상성** — RPi syncEMR 매 사이클 정상 동작 확인 (Next/React 변경은 서버에 영향 없지만 deploy webhook 정상 작동 검증 차원)

이상 발견 시 즉시 롤백:
- Vercel 대시보드 → Deployments → `dpl_GYWcusFGJXG1Lz1zZZqY9zJrk39b` (Next 15 + React 18 안정 버전) → "Promote to Production" → 1분 내 원복
- 또는 `git revert 18b25a9` (React 19 만 되돌리고 Next 15 유지) / `git revert 18b25a9 0bf657a` (둘 다 되돌리기) → push

---

## 다음 슬롯 (2026-05-09 토 오전) 작업 항목

### 단계 D — Next 15 → 16
- 사전 audit 필요:
  - **AMP 미사용 확인** (Next 16 에서 AMP 제거됨)
  - **`next lint` 사용 여부** (Next 16 에서 제거, `next-lint-to-eslint-cli` codemod 가능)
  - **`next/image` defaults 변경** — qualities `[75]`, `imageSizes` 에서 `16` 제거, `minimumCacheTTL` 4h, **`blockLocalIPs` 기본 활성화** (내부망 이미지 사용 시 영향)
  - **`serverRuntimeConfig`/`publicRuntimeConfig` 제거** — 사용 여부 grep
- codemod: `npx @next/codemod@canary upgrade latest`
- 위험도 🟡 Med (config 변경이라 audit 결과에 따라 위험도 재평가)

### 단계 E (선택) — Dependabot 잔여 정리
현재 2건(1 moderate, 1 low). 단계 D 까지 끝낸 뒤 어떤 transitive 인지 점검.
